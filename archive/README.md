# Archived utilities

`lookup-companies.mjs` is retained for reference only. It uses the retired 1–5 company/role scoring model and is not part of the supported job-search workflow.

The supported discovery path is documented in `docs/SYSTEM_MAP.md`.
